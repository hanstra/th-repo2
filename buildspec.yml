version: 0.2

phases:
  install:
    commands:
      - echo Nothing to do in the install phase...
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started on `date`
      - pwd
      - /bin/ls
      - which aws
      - aws s3 cp README.md s3://th-dest1
      - aws s3 cp *.yml s3://th-dest1
  post_build:
    commands:
      - echo Build completed on `date`
